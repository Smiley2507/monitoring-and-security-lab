[
  {% for host in groups['role_app'] | default([]) %}
  {
    "targets": ["{{ hostvars[host].private_ip_address }}:9113"],
    "labels": {
      "instance": "{{ host }}",
      "service": "nginx"
    }
  }{{ "," if not loop.last }}
  {% endfor %}
]
